#pragma once

#include <list>

#include "../Expressions/Expression.h"
#include "../Scanner/Scanner.h"

class Parser {
  public:
    virtual calcpp::expression parse(TokenCollection& tokens) const = 0;
};
