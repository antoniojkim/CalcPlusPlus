#pragma once

namespace calcpp {
    enum Type {
        NUM,
        INT,
        FLOAT,
        HEX,
        BIN,
        TUPLE,
        MATRIX,
        CALLABLE,
        STRING,
        UNIT,
        VAR,
        RANDIST,  // Random Distribution
        OPERATOR
    };
}
